#include "tests.h"

#include "../include/libs.h"
#include "../include/string_process.h"

void test_split_input_string(){
    printf("%s\n\n", "split_input_string()");

    // int N = 1;

    // const char given_input_strings[N];
    // given_input_strings[0] = "my input_string";
    // char correct_commands[N]
    // char *correct_commands[]

    // for(int i=0; i < N; i++){
    //     printf("------ Test %d ------\n", i+1);
    //     char *input_string = malloc(MAX_COMMAND_LENGTH * sizeof(char)); 
	//     char **commands = malloc(MAX_COMMANDS_COUNT * sizeof(char*));

    //     strcpy(input_string, given_input_strings[0]);

    //     printf("Given: \"%s\"\n", input_string);

    //     int number_of_commands = split_input_string(input_string, commands);
        
    //     printf("Result: \"%s\"\n", input_string);
    //     for(int j=0; j<number_of_commands; j++){
    //         printf("\"%s\" ", commands[j]);
    //     }
    //     printf("\n");

    //     free(input_string);
	//     free(commands);
    // }
}